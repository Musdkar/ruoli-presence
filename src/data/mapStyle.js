// MapLibre style URLs. The map is decorative; only theme varies the style.
const MAP_STYLE = {
  dark: "https://tiles.openfreemap.org/styles/dark",
  light: "https://tiles.openfreemap.org/styles/positron",
};

export default MAP_STYLE;
